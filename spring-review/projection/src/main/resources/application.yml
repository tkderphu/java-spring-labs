spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/thuc_tap
    username: postgres
    password: quangphu
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create  # or use validate, create, create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect